"use strict";

var exported = require('..');

describe('text exports', function () {
  Object.keys(exported).forEach(function (e) {
    it("".concat(e, " should be exported"), function () {
      expect(e).toBeDefined();
    });
  });
});