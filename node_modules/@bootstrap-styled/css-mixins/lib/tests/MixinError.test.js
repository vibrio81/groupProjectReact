"use strict";

var _MixinError = _interopRequireDefault(require("../MixinError"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

describe('MixinError', function () {
  it('should be throwable', function () {
    expect(function () {
      throw new _MixinError.default('error');
    }).toThrowError(new _MixinError.default('error'));
  });
  it('should have message', function () {
    var err = new _MixinError.default('error');
    expect(err.message).toEqual('error');
  });
});