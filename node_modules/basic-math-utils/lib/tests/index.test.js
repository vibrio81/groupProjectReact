'use strict';

var _index = require('../index');

describe('Math-utils', function () {
  it('rangeUtils should be exported', function () {
    expect(_index.rangeUtils.constructor.name).toEqual('RangeUtils');
  });
});